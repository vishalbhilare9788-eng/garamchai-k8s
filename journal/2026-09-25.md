# Journal: 2026-09-25 (Session 4: Phase 0 closed, Phase 1 requirements)

## What we did
- Phase 0 self-check completed; **Phase 0 ticked**. Gaps found and corrected on the way:
  - Capacity vs Allocatable: it's per node; Allocatable = Capacity − kube-reserved − system-reserved − eviction threshold; the scheduler compares **requests** against Allocatable.
  - DiskPressure taint: set by the **node lifecycle controller** (kube-controller-manager) from the kubelet's DiskPressure condition, not by kube-proxy.
  - Namespaced vs cluster-scoped is a property of the object **kind**, not of a namespace. `kube-system` is an ordinary namespace. Role is namespaced (ClusterRole isn't).
- Phase 1 started: charter §6 **Functional** and **Non-functional** sections decided (3 roles, F1–F13, order lifecycle, N1–N5: 99.5 %, RPO 4 h, pay at the counter).
- Vishal revealed they're a Cloud Ops Manager (14 yrs); the intern role-play continues. Side topic on request: study guide `docs/guides/high-availability-99.99.pdf` (PayKaro, a financial app on AWS at 99.99 %).

## Key decisions & why
| Decision | Why |
|---|---|
| Login required, opt-in consent for offers | Contact details for updates; DPDP/GDPR need consent |
| Prevent instead of reject (open/closed + stock flags) | Customers shouldn't place orders that get rejected |
| No substitution negotiation, pickup only | Code complexity that teaches no Kubernetes object; no delivery staff |
| 99.5 %, RPO 4 h (Postgres backup CronJob every 4 h) | ₹5,000 per hour of downtime doesn't justify more; a day's orders is too much to lose |

## ▶ Resume here
Phase 1, **Security part 1**: proposal S1–S5 (bcrypt, Secrets not in Git, encryption at rest, a DB user per service, TLS at the edge + NetworkPolicy) is waiting for Vishal's accept/changes, plus two one-line answers (why a hash; why a Secret isn't secret). Then Security part 2 (cluster access), then Operations, then architecture v1.0.

## Later the same session
- Phase 1 Security decided: S1–S5 (data protection) and A1–A5 (access); deploys run from the laptop, CI only builds images.
- Vishal's answers: 401 vs 403 correct (with HTTP codes). Needed correcting: the group comes from the certificate's `O=` field (admin.conf is only the wallet); a stolen certificate is worse because client certs **can't be revoked**; encryption at rest uses a **symmetric** key, not a private key.
- Built phase-wise study PDFs in `docs/study/` (5 PDFs + index, rebuilt with `bash docs/study/build.sh`).

## ▶ Resume here (updated)
Phase 1: **Operations** section of the questionnaire (releases/rollback, logging, monitoring), then architecture v1.0 + ADRs.
